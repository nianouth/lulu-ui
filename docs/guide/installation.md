# 安装

## 环境要求

- Node.js >= 18
- Vue >= 3.4

## 包管理器

推荐使用 **pnpm** 作为包管理器。

```bash
# 安装 pnpm
npm install -g pnpm
```

## 安装组件库

```bash
pnpm add @lulu-ui/components
```

## 图表组件

如果需要使用图表组件（LineChart / BarChart / PieChart），需要额外安装 ECharts：

```bash
pnpm add echarts
```

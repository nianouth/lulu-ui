{
  "name": "lulu-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Lulu UI - Vue 3 组件库",
  "scripts": {
    "dev": "pnpm -C packages/components dev",
    "build": "pnpm -C packages/components build",
    "docs:dev": "pnpm -C docs dev",
    "docs:build": "pnpm -C docs build",
    "docs:preview": "pnpm -C docs preview"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "echarts": "^6.0.0"
  }
}
